class botApiService{
    static getBotResponse(userMessage: string): string{
        const botResponse: string = "sample response";
            return botResponse;
    }
}
export default botApiService;